---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: ovsdpdkresourcepolicies.ovsdpdk.k8snetworkplumbingwg.io
spec:
  group: ovsdpdk.k8snetworkplumbingwg.io
  names:
    kind: OvsDpdkResourcePolicy
    listKind: OvsDpdkResourcePolicyList
    plural: ovsdpdkresourcepolicies
    singular: ovsdpdkresourcepolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          OvsDpdkResourcePolicy defines the policy for advertising OVS bridges that the
          driver will expose as DRA devices.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OvsDpdkResourcePolicySpec defines the desired state of OvsDpdkResourcePolicy.
            properties:
              bridges:
                description: Bridges is the list of OVS bridges exposed as DRA devices
                  by this policy.
                items:
                  description: BridgeSpec defines a single OVS bridge to be exposed
                    as a DRA device.
                  properties:
                    name:
                      description: Name is the name of the OVS bridge.
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
              nodeSelector:
                description: |-
                  NodeSelector restricts the nodes to which this policy applies.
                  If not set, the policy applies to all nodes.
                properties:
                  nodeSelectorTerms:
                    description: Required. A list of node selector terms. The terms
                      are ORed.
                    items:
                      description: |-
                        A null or empty node selector term matches no objects. The requirements of
                        them are ANDed.
                        The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                      properties:
                        matchExpressions:
                          description: A list of node selector requirements by node's
                            labels.
                          items:
                            description: |-
                              A node selector requirement is a selector that contains values, a key, and an operator
                              that relates the key and values.
                            properties:
                              key:
                                description: The label key that the selector applies
                                  to.
                                type: string
                              operator:
                                description: |-
                                  Represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                type: string
                              values:
                                description: |-
                                  An array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. If the operator is Gt or Lt, the values
                                  array must have a single element, which will be interpreted as an integer.
                                  This array is replaced during a strategic merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchFields:
                          description: A list of node selector requirements by node's
                            fields.
                          items:
                            description: |-
                              A node selector requirement is a selector that contains values, a key, and an operator
                              that relates the key and values.
                            properties:
                              key:
                                description: The label key that the selector applies
                                  to.
                                type: string
                              operator:
                                description: |-
                                  Represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                type: string
                              values:
                                description: |-
                                  An array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. If the operator is Gt or Lt, the values
                                  array must have a single element, which will be interpreted as an integer.
                                  This array is replaced during a strategic merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                    x-kubernetes-list-type: atomic
                required:
                - nodeSelectorTerms
                type: object
                x-kubernetes-map-type: atomic
              vhostUser:
                description: |-
                  VhostUser configures the vhost-user socket directory paths used when
                  preparing resource claims on this node.
                properties:
                  aclUsers:
                    description: |-
                      ACLUsers is a list of user names granted access to the socket directory
                      via filesystem ACLs (setfacl).
                    items:
                      type: string
                    type: array
                  containerRootPath:
                    description: |-
                      ContainerRootPath is the path inside the container where the per-pod
                      socket directory is mounted. Defaults to DefaultContainerRootPath.
                    type: string
                  group:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      Group is the owning group of the vhost-user socket directory. Can be
                      specified as a group name (e.g. "qemu") or a numeric GID (e.g. 718).
                    x-kubernetes-int-or-string: true
                  hostRootPath:
                    description: |-
                      HostRootPath is the root directory on the host under which per-pod socket
                      directories are created. Defaults to DefaultHostRootPath.
                    type: string
                  selinuxLabel:
                    description: SelinuxLabel is the SELinux label applied to the
                      socket directory.
                    type: string
                  user:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      User is the owner of the vhost-user socket directory. Can be specified
                      as a user name (e.g. "openvswitch") or a numeric UID (e.g. 107).
                    x-kubernetes-int-or-string: true
                type: object
            required:
            - bridges
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
